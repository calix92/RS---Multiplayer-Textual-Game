syntax = "proto3";

service PeerDiscovery {
    rpc RegisterPeer (Peer) returns (GetPeers);
    rpc NotifyPeer (Peer) returns (Empty);
}

message Peer {
  string uuid = 1;
  string address = 2;
}

message GetPeers {
    map<string, string> peers = 1;
}

message Empty {}
